#include <stdio.h>

/**
 * main - Prints a string followed by a new line using puts.
 *
 * Return: Always 0 (Success)
 */
	int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
