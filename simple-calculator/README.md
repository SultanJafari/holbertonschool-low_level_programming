# Simple Calculator

A terminal-based interactive calculator application written in C. This project is part of a low-level programming curriculum, focusing on building a complete program from scratch using standard C libraries.

## Compile Command
To compile the program with strict compiler flags, use the following command:

```bash
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 calculator.c -o calculator
